# Platform API URL
# For local development with port-forward, use:
# VITE_API_URL=http://localhost:8081
# For cluster deployment, this defaults to the service DNS name
VITE_API_URL=http://platform-api.platform.svc.cluster.local

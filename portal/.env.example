# Platform API URL
# For production (proxied via nginx): leave empty or omit (default: '')
# For local development with port-forward: VITE_API_URL=http://localhost:8081
# Empty string means same-origin requests — nginx proxies /api/* to platform-api service
VITE_API_URL=

# Platform API Bearer Token
# For production: leave empty to use default 'homelab-portal-token'
# For custom token: VITE_API_TOKEN=your-token-here
# TODO: Replace with ExternalSecret + runtime injection for production
VITE_API_TOKEN=
